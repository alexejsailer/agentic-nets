# AgetnticOS Agentic-Nets — Environment Variables
# Copy this file to .env and fill in your values:
#   cp .env.template .env
#
# =============================================================================
# Image Version (all services use the same release version)
# =============================================================================
AGENTICOS_VERSION=1.21.1

# =============================================================================
# Core Service Ports
# =============================================================================
AGENTIC_NET_NODE_PORT=8080
AGENTIC_NET_MASTER_PORT=8082
AGENTIC_NET_GATEWAY_PORT=8083
AGENTIC_NET_EXECUTOR_PORT=8084
AGENTIC_NET_GUI_PORT=4200
SA_BLOBSTORE_PORT=8090
AGENTIC_NET_VAULT_PORT=8085

# =============================================================================
# Monitoring Ports
# =============================================================================
GRAFANA_PORT=3000
PROMETHEUS_PORT=9090
TEMPO_PORT=3200
OTEL_COLLECTOR_GRPC_PORT=4317
OTEL_COLLECTOR_HTTP_PORT=4318

# =============================================================================
# LLM Configuration
# =============================================================================
# For a step-by-step walkthrough see https://agentic-nets.com/install
#
# You MUST configure ONE provider before the stack will work:
#   - "ollama"  — default, free/offline, needs Ollama running on your host
#   - "claude"  — Anthropic API, needs ANTHROPIC_API_KEY (pay per use)
#
# Provider: "ollama", "claude", "openai", "claude-code", "codex"
LLM_PROVIDER=ollama
AGENTICOS_MODEL_TIER=medium

# --- Ollama ---
# Recommended default: deepseek-v3.1:671b-cloud (strong reasoning, cloud-backed).
# Cloud models require `ollama login` on the host. They're rate-limited by
# ollama.com, so under heavy concurrent-agent load (e.g. the onboarding net
# firing 4 agents in parallel) you may see 429s — fall back to a LOCAL model
# for those workloads: llama3.2, qwen2.5, mistral (`ollama pull <name>`).
OLLAMA_BASE_URL=http://host.docker.internal:11434
OLLAMA_HIGH_MODEL=deepseek-v3.1:671b-cloud
OLLAMA_MEDIUM_MODEL=deepseek-v3.1:671b-cloud
OLLAMA_LOW_MODEL=deepseek-v3.1:671b-cloud

# --- Anthropic (required if LLM_PROVIDER=claude) ---
# Get a key at https://console.anthropic.com
ANTHROPIC_API_KEY=
ANTHROPIC_HIGH_MODEL=claude-opus-4-6
ANTHROPIC_MEDIUM_MODEL=claude-sonnet-4-6
ANTHROPIC_LOW_MODEL=claude-haiku-4-5-20251001

# --- OpenAI ---
OPENAI_API_KEY=
OPENAI_HIGH_MODEL=gpt-5.2
OPENAI_MEDIUM_MODEL=o4-mini
OPENAI_LOW_MODEL=gpt-5-nano

# =============================================================================
# Security & Credentials
# =============================================================================
# OAuth2 admin secret (auto-generated by gateway on first startup if empty)
AGENTICOS_ADMIN_SECRET=

# Encryption key for persisted system settings (generate a random 64-char hex string)
AGENTICOS_SETTINGS_KEY=

# OpenBao dev root token (used by vault service and master)
OPENBAO_DEV_ROOT_TOKEN=agenticos-dev-token    # CHANGE ME — dev mode only, use a random token in production

# Grafana dashboard password
GRAFANA_ADMIN_PASSWORD=admin

# =============================================================================
# Telegram Bot (optional)
# =============================================================================
TELEGRAM_BOT_ENABLED=false
TELEGRAM_BOT_TOKEN=
TELEGRAM_BOT_USERNAME=
TELEGRAM_BOT_WEBHOOK_BASE_URL=
TELEGRAM_BOT_WEBHOOK_SECRET=
TELEGRAM_BOT_ALLOWED_CHAT_IDS=

# =============================================================================
# Data Persistence
# =============================================================================
# Host directory for node data (events + snapshots)
AGENTICOS_NODE_DATA_DIR=~/.agenticos

# =============================================================================
# Resource Limits
# =============================================================================
AGENTIC_NET_NODE_MEMORY_LIMIT=2G
AGENTIC_NET_NODE_CPU_LIMIT=1.0
AGENTIC_NET_MASTER_MEMORY_LIMIT=2G
AGENTIC_NET_MASTER_CPU_LIMIT=1.0
AGENTIC_NET_EXECUTOR_MEMORY_LIMIT=1G
AGENTIC_NET_EXECUTOR_CPU_LIMIT=0.75
AGENTIC_NET_GATEWAY_MEMORY_LIMIT=1G
AGENTIC_NET_GATEWAY_CPU_LIMIT=0.75
AGENTICOS_CLI_MEMORY_LIMIT=512M
AGENTICOS_CLI_CPU_LIMIT=0.5
AGENTICOS_CHAT_MEMORY_LIMIT=512M
AGENTICOS_CHAT_CPU_LIMIT=0.5
AGENTIC_NET_VAULT_MEMORY_LIMIT=512M
AGENTIC_NET_VAULT_CPU_LIMIT=0.5

# =============================================================================
# Spring Configuration
# =============================================================================
SPRING_PROFILES_ACTIVE=docker
