{
  "cases": [
    {"name": "scripts registered", "check": "the target model's local tool catalog contains steward-infra, steward-observe, steward-context, steward-gate, steward-apply, steward-verify, steward-release and steward-brain", "why": "command lanes invoke by toolId, resolved per model; a missing entry fails at fire time"},
    {"name": "charter seeded once", "check": "p-steward-charter holds one token with charterId steward; a reinstall does not add a second", "why": "seeds are templates; a place that already holds a token keeps it"},
    {"name": "the person is asked first", "check": "after install and one observe, p-steward-prompts holds a prompt in goal mode and nothing else moved", "why": "every iteration ends in a question; with an empty goal the first one asks for it"},
    {"name": "the gate refuses the protected set", "check": "a spec whose touches name t-steward-gate-cmd lands in p-steward-refused with a reason, never in p-steward-decisions", "why": "the Steward improves every net except the ones that govern it"},
    {"name": "a bad version rolls back", "check": "a spec whose verification fails leaves the model on the previous pack version and the run marked rolled-back", "why": "every change is a pack version and the previous one is the rollback"}
  ]
}
