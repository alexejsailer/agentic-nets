[
  {
    "kind": "policy",
    "allowedModels": "academy,net-lab",
    "protectedPlaces": "p-inspector-*, p-insp-*, *-charter, *-config, *-policy",
    "gateRegex": "^(academy|net-lab) (?!p-inspector-)(?!p-insp-)(?!.*-charter$)(?!.*-config$)(?!.*-policy$).+$",
    "gateInput": "<targetModel> <place> - one anchored regex enforcing BOTH the model allowlist and the protected-place denylist. Group 1 is the model, so a match yields a non-DENY verdict.",
    "note": "Widen deliberately. Enforced by a gate transition, not by the charter - a direct place read through the pack cannot bypass it. Reading is disclosure: this pack refuses the same targets a deletion pack would."
  }
]
