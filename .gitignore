# Java build artifacts
target/
classes/
**/target/
**/classes/
*.class

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
dist/

# IDE files
.idea/
*.iml
.vscode/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Logs
*.log

# Environment variables - NEVER commit these
.env
.env.local
.env.development
.env.production
**/.env
**/.env.local
**/.env.development
**/.env.production
# Local .env backups (may contain real secrets) — never commit
.env.backup*
**/.env.backup*
.env.bak
**/.env.bak

# Deployment data and runtime secrets
deployment/data/
**/data/jwt/

# Agent field-report working notes (source material for improvement waves, not documentation)
agentic-net-mcp/improvement*.md

# Machine-local compose overrides (host IPs, local image pins, mounted credentials).
# Shared overrides — native-master, master-local, test — stay tracked.
deployment/docker-compose.cartographer.override.yml
deployment/docker-compose.executor-local.override.yml
deployment/docker-compose.mac-host-ollama.override.yml
deployment/docker-compose.tailscale.override.yml
deployment/config/llm-groups.json
deployment/config/llm-providers.env
